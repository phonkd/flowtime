# Application Config
NODE_ENV=development
PORT=5000

# Session Secret (replace with a strong random string in production)
SESSION_SECRET=your_secure_session_secret_key_here

# Storage Configuration
# Set to 'true' to use PostgreSQL database, 'false' to use in-memory storage
USE_DATABASE=false

# Database Configuration (only needed when USE_DATABASE=true)
# If using the docker-compose setup, this will work out of the box
DATABASE_URL=postgresql://hypnosis:hypnosis_password@postgres:5432/hypnosis_db

# For local development without Docker, use:
# DATABASE_URL=postgresql://localhost:5432/hypnosis_db